package j05_classMethod;

// ** MyClass
// => 맴버변수 3개, 메서드 2개 정의
// => MyClass 를 정의하고
//    Ex02_MyClassTest 에서 인스턴스 2개 만들어 출력하기

class Throne {
	// ** 맴버 변수
	// => 전역(Global) 변수
	public long price;
	public int weight;
	public String color;
	
	public void priceUp(long mymoney) {
		price += 100000000; // 전역변수
		System.out.println("** priceUp price1 = " + price);
		// ** 지역(local)변수
		// => 내부에 정의된 변수, 인자
		// {   } 내부에서만 적용가능
		myprice += price;
		long money = 100000000;
		long price = 50000000; //지역변수
		System.out.println("** priceUp price2 = " + price);
		
	}
	
	public void priceDown() {
		price -= 100000000;
	}
	
	public String toString() {
		return "[ 가격: " + price + ", 무게: " + weight + ", 색상: " + color + " ]";
	}
} //throne

public class Ex02_MyClassTest {

	public static void main(String[] args) {
		Throne JkThrone = new Throne();
		JkThrone.price = 200000000;
		JkThrone.weight = 1000;
		JkThrone.color = "black";
		System.out.println("** JK의 왕좌 ** => " + JkThrone.toString());
		
		// ** 지역변수 Test
		JkThrone.priceUp(50000000);
		
		Throne JSThrone = new Throne();
		JSThrone.price = 30000000000L;
		JSThrone.weight = 2000;
		JSThrone.color = "Dark_gray";
		System.out.println("** JS의 왕좌 ** => " + JSThrone);
		
	} //main

} //class
